---
import Base from "../layouts/Base.astro";
import TinaIsland from "@tinacms/astro/TinaIsland.astro";
import HomepagePage from "../components/HomepagePage.astro";
import { getStaticHomepagePage } from "../lib/static-content";
import { homepageWrapper } from "../lib/tina/wrappers";

const page = getStaticHomepagePage();
const data = page.data;
const homepage = data.homepage;
const settings = data.settings;
---

<Base
  title={homepage.seo.title}
  description={homepage.seo.description}
  homepage={homepage}
  settings={settings}
>
  <TinaIsland name="homepage" wrapper={homepageWrapper} primary>
    <HomepagePage data={data} />
  </TinaIsland>
</Base>
